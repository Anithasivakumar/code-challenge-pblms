def gcd(a,b):
    if(b==0):
        return a
    else:
        return gcd(b,a%b)
    if(c==0):
        return d
    else:
        return gcd(c,d%c)
a,b,c=map(int,input().split())
d=gcd(a,b)
GCD=gcd(d,c)
print(GCD)
    
    
