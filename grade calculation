a=int(input())
b=list(map(int,input().split()))[0:a]
for i in  range(0,len(b)):
        c=int(((b[i]+2)/5)*5)
        d=c-b[i]
        if(d<c):
            print(c)
        else:
            print(b[i])
        
    
